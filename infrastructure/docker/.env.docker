# =============================================================================
# SPANCLE SPORTS OS — Docker Compose Environment Template
# infrastructure/docker/.env.docker
# =============================================================================
# Copy to infrastructure/docker/.env for local docker usage.
# NEVER commit .env — only this .env.docker template.
# =============================================================================

POSTGRES_USER=spancle
POSTGRES_PASSWORD=CHANGE_ME_DOCKER
POSTGRES_DB=spancle_dev
POSTGRES_PORT=5432

REDIS_PASSWORD=CHANGE_ME_DOCKER_REDIS
REDIS_PORT=6379
