{
  "name": "@spancle/constants",
  "version": "0.1.0",
  "private": true,
  "description": "Spancle Sports OS — Platform-wide immutable constants",
  "license": "UNLICENSED",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings=0",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@spancle/tsconfig": "workspace:*",
    "@spancle/eslint-config": "workspace:*",
    "rimraf": "*",
    "typescript": "*"
  }
}
